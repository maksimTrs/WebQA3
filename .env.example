# --- Application ---
BASE_URL=https://webqa.mercdev.com
API_BASE_URL=https://webqa.mercdev.com/api/v1

# --- Test user (must exist on the target environment) ---
TEST_USER_EMAIL=test1001@mail.com
TEST_USER_PASSWORD=020590

# --- Logging ---
# 'debug' | 'info' | 'warn' | 'error'  (default: info)
LOG_LEVEL=info

# Mask sensitive values (token, password, email) in console + report (default: true)
MASK_SENSITIVE=true
